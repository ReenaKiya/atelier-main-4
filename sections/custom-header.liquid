{%- liquid
  assign logo = section.settings.logo
  assign search_placeholder = section.settings.search_placeholder
-%}

<header
  class="custom-header"
  style="
    --ch-bg: {{ section.settings.background_color }};
    --ch-text: {{ section.settings.text_color }};
    --ch-border: {{ section.settings.border_color }};
    --ch-accent: {{ section.settings.accent_color }};
    --ch-logo-height: {{ section.settings.logo_height }}px;
  "
>
  <div class="custom-header__inner page-width">
    <!-- Hamburger (mobile only) -->
    <button
      type="button"
      class="custom-header__hamburger"
      aria-label="Open menu"
      onclick="window.openCustomMobileMenu && window.openCustomMobileMenu();"
    >
      <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <line x1="3" y1="6" x2="21" y2="6"></line>
        <line x1="3" y1="12" x2="21" y2="12"></line>
        <line x1="3" y1="18" x2="21" y2="18"></line>
      </svg>
    </button>

    <!-- Logo -->
    <div class="custom-header__logo">
      <a href="/" class="custom-header__logo-link">
        {%- if logo != blank -%}
          {{ logo | image_url: width: 400 | image_tag: class: 'custom-header__logo-img', loading: 'eager', alt: shop.name }}
        {%- else -%}
          <span class="custom-header__logo-text">{{ shop.name }}</span>
        {%- endif -%}
      </a>
    </div>

    {% comment %}
      Centre slot. The header-navigation section renders #site-header-nav in its
      own section below; the script at the bottom of this file relocates that
      element into here so the menu sits on the same row as the logo.
    {% endcomment %}
    <div class="custom-header__nav" data-header-nav-slot></div>

    <!-- Icons -->
    <div class="custom-header__icons">
      <button type="button" class="custom-header__icon custom-header__search-toggle" aria-label="Search" aria-expanded="false" data-ch-search-toggle>
        <svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="11" cy="11" r="8"></circle>
          <line x1="21" y1="21" x2="16.65" y2="16.65"></line>
        </svg>
      </button>

      {%- if section.settings.show_account and shop.customer_accounts_enabled -%}
        <a href="{%- if customer -%}{{ routes.account_url }}{%- else -%}{{ routes.account_login_url }}{%- endif -%}" class="custom-header__icon" aria-label="Account">
          <svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path>
            <circle cx="12" cy="7" r="4"></circle>
          </svg>
        </a>
      {%- endif -%}

      {%- if section.settings.show_wishlist -%}
        <a href="{{ section.settings.wishlist_url | default: '/pages/wishlist' }}" class="custom-header__icon" aria-label="Wishlist">
          <svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"></path>
          </svg>
        </a>
      {%- endif -%}

      <!-- Side Cart Drawer -->
      <div class="custom-cart-drawer-wrapper">
        <button class="custom-header__icon custom-header__cart" aria-label="Open cart" onclick="document.getElementById('custom-cart-drawer').classList.add('is-open'); document.getElementById('custom-cart-overlay').classList.add('is-open');">
          <svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <path d="M6 2L3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"></path>
            <line x1="3" y1="6" x2="21" y2="6"></line>
            <path d="M16 10a4 4 0 0 1-8 0"></path>
          </svg>
          <span class="custom-header__cart-count" data-cart-count>{%- if cart.item_count > 0 -%}{{ cart.item_count }}{%- endif -%}</span>
        </button>
      </div>
    </div>
  </div>

  <!-- Search panel — toggled by the magnifier icon -->
  <div class="custom-header__search" data-ch-search hidden>
    <div class="custom-header__search-inner page-width">
      <form action="/search" method="get" class="custom-header__search-form">
        <input
          type="search"
          name="q"
          class="custom-header__search-input"
          placeholder="{{ search_placeholder }}"
          autocomplete="off"
          data-ch-search-input
        >
        <button type="submit" class="custom-header__search-btn" aria-label="Search">
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="11" cy="11" r="8"></circle>
            <line x1="21" y1="21" x2="16.65" y2="16.65"></line>
          </svg>
        </button>
      </form>

      <!-- Predictive Search Dropdown -->
      <div class="ch-search-results" data-ch-search-results hidden>
        <div class="ch-search-loading" data-ch-search-loading hidden>Searching...</div>
        <div class="ch-search-content" data-ch-search-content></div>
      </div>
    </div>
  </div>
</header>

<script>
  // Relocate the header-navigation menu into the header's centre slot so the
  // logo, menu and icons share one row (matches the live Orionz header).
  (function () {
    function mountNav() {
      var slot = document.querySelector('[data-header-nav-slot]');
      var nav = document.getElementById('site-header-nav');
      if (!slot || !nav || slot.contains(nav)) return;
      slot.appendChild(nav);
      document.documentElement.classList.add('ch-nav-mounted');
    }
    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', mountNav);
    } else {
      mountNav();
    }
    // Theme editor re-renders either section independently — re-mount after both.
    document.addEventListener('shopify:section:load', mountNav);
  })();

  // Search icon toggles the search panel
  (function () {
    if (window.__chSearchToggleInit) return;
    window.__chSearchToggleInit = true;

    document.addEventListener('click', function (e) {
      var toggle = e.target.closest('[data-ch-search-toggle]');
      var panel = document.querySelector('[data-ch-search]');
      if (!panel) return;

      if (toggle) {
        e.preventDefault();
        e.stopPropagation();
        var open = panel.hasAttribute('hidden');
        if (open) {
          panel.removeAttribute('hidden');
          toggle.setAttribute('aria-expanded', 'true');
          var input = panel.querySelector('[data-ch-search-input]');
          if (input) input.focus();
        } else {
          panel.setAttribute('hidden', '');
          toggle.setAttribute('aria-expanded', 'false');
        }
        return;
      }

      // Click outside the panel closes it
      if (!panel.hasAttribute('hidden') && !e.target.closest('[data-ch-search]')) {
        panel.setAttribute('hidden', '');
        var t = document.querySelector('[data-ch-search-toggle]');
        if (t) t.setAttribute('aria-expanded', 'false');
      }
    });

    document.addEventListener('keydown', function (e) {
      if (e.key !== 'Escape') return;
      var panel = document.querySelector('[data-ch-search]');
      if (panel && !panel.hasAttribute('hidden')) {
        panel.setAttribute('hidden', '');
        var t = document.querySelector('[data-ch-search-toggle]');
        if (t) t.setAttribute('aria-expanded', 'false');
      }
    });
  })();
</script>

<!-- Mobile Menu Overlay -->
<div
  id="custom-mobile-menu-overlay"
  class="custom-mobile-menu-overlay"
  onclick="document.getElementById('custom-mobile-menu').classList.remove('is-open'); this.classList.remove('is-open');"
></div>

<!-- Mobile Menu Drawer -->
<div id="custom-mobile-menu" class="custom-mobile-menu">
  <div class="custom-mobile-menu__header">
    <span class="custom-mobile-menu__title">Menu</span>
    <button
      type="button"
      class="custom-mobile-menu__close"
      aria-label="Close menu"
      onclick="document.getElementById('custom-mobile-menu').classList.remove('is-open'); document.getElementById('custom-mobile-menu-overlay').classList.remove('is-open');"
    >
      <svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <line x1="18" y1="6" x2="6" y2="18"></line>
        <line x1="6" y1="6" x2="18" y2="18"></line>
      </svg>
    </button>
  </div>
  <div class="custom-mobile-menu__nav" aria-label="Main" data-mobile-menu-body>
    {% comment %} Content is injected from #site-header-nav (header-navigation section) on hamburger click {% endcomment %}
  </div>
  {% comment %}
  <nav class="custom-mobile-menu__nav" aria-label="Main">
    <ul class="navmenu navmenu-depth-1" data-navmenu="" aria-label="Menu Hierarchy" style="display:flex;align-items:center;height:auto;margin:0;">

      {% for link in linklists[section.settings.mobile_menu].links %}
        {% assign link_handle = link.title | handleize %}

        {% if link.links.size > 0 %}
          {% comment %} ========== MEGA MENU ITEM ========== {% endcomment %}
          {% assign meganav_id = 'meganav_mobile_' | append: section.id | append: '_' | append: forloop.index %}

          <li class="navmenu-item navmenu-item-parent navmenu-meganav__item-parent navmenu-id-{{ link_handle }}"
              data-navmenu-meganav-trigger=""
              data-navmenu-meganav-type="multi-column-menu"
              data-navmenu-parent=""
              style="display:flex;align-items:center;padding-top:0;height:auto;--menu-closed-height:0;">
            <details data-navmenu-details="">
              <summary class="navmenu-link navmenu-link-depth-1 navmenu-link-parent"
                       aria-haspopup="true"
                       aria-expanded="false"
                       data-href="{{ link.url }}">
                {{ link.title }}
                <span class="navmenu-icon navmenu-icon-depth-1" data-navmenu-trigger="">
                  <svg aria-hidden="true" focusable="false" role="presentation" width="8" height="6" viewBox="0 0 8 6" fill="none" xmlns="http://www.w3.org/2000/svg" class="icon-chevron-down" style="animation:none;">
                    <path class="icon-chevron-down-left" d="M4 4.5L7 1.5" stroke="currentColor" stroke-width="1.25" stroke-linecap="square"></path>
                    <path class="icon-chevron-down-right" d="M4 4.5L1 1.5" stroke="currentColor" stroke-width="1.25" stroke-linecap="square"></path>
                  </svg>
                </span>
              </summary>

              <div class="navmenu-meganav navmenu-meganav--desktop navmenu-meganav-multi-column"
                   data-navmenu-submenu=""
                   data-meganav-menu=""
                   data-meganav-id="{{ meganav_id }}"
                   data-meganav-multi-column=""
                   style="display:none;opacity:0;visibility:hidden;">
                <div class="multi-col-wrapper">

                  {% comment %} Left Column: Categories (Level 2 links) {% endcomment %}
                  <div class="multi-col-categories">
                    <p class="multi-col-categories-title">Browse by category</p>
                    <ul class="multi-col-category-list">
                      {% for child_link in link.links %}
                        <li class="multi-col-category-item">
                          <a href="{{ child_link.url }}"
                             class="multi-col-category-link{% if forloop.first %} active{% endif %}"
                             data-category-index="{{ forloop.index }}">
                            <span class="multi-col-category-text">{{ child_link.title }}</span>
                            <svg class="multi-col-category-arrow" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="animation:none;">
                              <polyline points="9 18 15 12 9 6"></polyline>
                            </svg>
                          </a>
                        </li>
                      {% endfor %}
                    </ul>
                  </div>

                  {% comment %} Middle Column: Submenus (Level 3 links) {% endcomment %}
                  <div class="multi-col-submenus">
                    {% for child_link in link.links %}
                      <div class="multi-col-submenu-panel{% if forloop.first %} active{% endif %}" data-submenu-panel="{{ forloop.index }}">
                        <div class="multi-col-submenu-grid">
                          {% for grandchild_link in child_link.links %}
                            <a href="{{ grandchild_link.url }}" class="multi-col-submenu-link">{{ grandchild_link.title }}</a>
                          {% endfor %}
                          <a href="{{ child_link.url }}" class="multi-col-submenu-link multi-col-submenu-link--view-all">View All {{ child_link.title }} &rarr;</a>
                        </div>
                      </div>
                    {% endfor %}
                  </div>

                  {% comment %} Right Column: Featured (images + products per category) {% endcomment %}
                  <div class="multi-col-featured">
                    <p class="multi-col-featured-title">Featured</p>

                    {% for child_link in link.links %}
                      {% assign child_handle = child_link.title | handleize %}
                      <div class="multi-col-featured-panel{% if forloop.first %} active{% endif %}" data-featured-panel="{{ forloop.index }}">

                        {% comment %} Featured Images for this category {% endcomment %}
                        {% for block in section.blocks %}
                          {% if block.type == 'featured_image' %}
                            {% assign block_parent_handle = block.settings.parent_menu_title | handleize %}
                            {% assign block_category_handle = block.settings.category_title | handleize %}
                            {% if block_parent_handle == link_handle and block_category_handle == child_handle and block.settings.image != blank %}
                              <a href="{{ block.settings.link | default: child_link.url }}" class="multi-col-featured-item" {{ block.shopify_attributes }}>
                                <div class="multi-col-featured-image-wrapper">
                                  <img src="{{ block.settings.image | image_url: width: 200 }}"
                                       alt="{{ block.settings.image.alt | default: block.settings.heading | escape }}"
                                       class="multi-col-featured-image"
                                       loading="lazy">
                                  <div class="multi-col-featured-content">
                                    {% if block.settings.heading != blank %}
                                      <p class="multi-col-featured-item-title">{{ block.settings.heading }}</p>
                                    {% endif %}
                                    {% if block.settings.subtext != blank %}
                                      <span class="multi-col-featured-subtext">{{ block.settings.subtext }}</span>
                                    {% endif %}
                                    <span class="multi-col-featured-cta">
                                      {{ block.settings.cta_text | default: 'Shop Now' }}
                                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="animation:none;">
                                        <polyline points="9 18 15 12 9 6"></polyline>
                                      </svg>
                                    </span>
                                  </div>
                                </div>
                              </a>
                            {% endif %}
                          {% endif %}
                        {% endfor %}

                        {% comment %} Featured Products for this category {% endcomment %}
                        {% for block in section.blocks %}
                          {% if block.type == 'featured_product' %}
                            {% assign block_parent_handle = block.settings.parent_menu_title | handleize %}
                            {% assign block_category_handle = block.settings.category_title | handleize %}
                            {% if block_parent_handle == link_handle and block.settings.product != blank %}
                              {% assign show_product = false %}
                              {% if block.settings.category_title == blank %}
                                {% assign show_product = true %}
                              {% elsif block_category_handle == child_handle %}
                                {% assign show_product = true %}
                              {% endif %}
                              {% if show_product %}
                                {% assign feat_product = block.settings.product %}
                                <a href="{{ feat_product.url }}" class="multi-col-featured-item" {{ block.shopify_attributes }}>
                                  <div class="multi-col-featured-image-wrapper">
                                    {% if feat_product.featured_image %}
                                      <img src="{{ feat_product.featured_image | image_url: width: 200 }}"
                                           alt="{{ feat_product.featured_image.alt | escape }}"
                                           class="multi-col-featured-image"
                                           loading="lazy">
                                    {% endif %}
                                    <div class="multi-col-featured-content">
                                      <p class="multi-col-featured-item-title">{{ feat_product.title | truncate: 45 }}</p>
                                      <span class="multi-col-featured-price">{{ feat_product.price | money }}</span>
                                      <span class="multi-col-featured-cta">
                                        Shop Now
                                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="animation:none;">
                                          <polyline points="9 18 15 12 9 6"></polyline>
                                        </svg>
                                      </span>
                                    </div>
                                  </div>
                                </a>
                              {% endif %}
                            {% endif %}
                          {% endif %}
                        {% endfor %}

                      </div>
                    {% endfor %}
                  </div>

                </div>
              </div>
            </details>
          </li>

        {% else %}
          {% comment %} ========== SIMPLE LINK ========== {% endcomment %}
          <li class="navmenu-item navmenu-basic__item navmenu-id-{{ link_handle }}" style="display:flex;align-items:center;padding-top:0;height:auto;">
            <a class="navmenu-link navmenu-link-depth-1" href="{{ link.url }}">{{ link.title }}</a>
          </li>
        {% endif %}
      {% endfor %}

    </ul>
  </nav>
  {% endcomment %}
</div>

<script>
  (function() {
    var menu = document.getElementById('custom-mobile-menu');
    if (!menu) return;
    menu.querySelectorAll('[data-mobile-menu-toggle]').forEach(function(toggle) {
      toggle.addEventListener('click', function(e) {
        e.preventDefault();
        var item = toggle.closest('.custom-mobile-menu__item');
        if (item) item.classList.toggle('is-open');
      });
    });
  })();

  // Open mobile drawer with header-navigation content cloned in
  window.openCustomMobileMenu = function() {
    var drawer = document.getElementById('custom-mobile-menu');
    var overlay = document.getElementById('custom-mobile-menu-overlay');
    var body = drawer && drawer.querySelector('[data-mobile-menu-body]');
    var source = document.getElementById('site-header-nav');

    if (body && source && !body.dataset.populated) {
      // Clone the header-navigation markup so the live desktop nav stays intact
      var clone = source.cloneNode(true);
      clone.removeAttribute('id');
      // Force inner nav/menus to be visible inside the drawer regardless of desktop CSS
      clone.querySelectorAll('[style]').forEach(function(el) {
        if (el.style.display === 'none') el.style.display = '';
        if (el.style.visibility === 'hidden') el.style.visibility = '';
        if (el.style.opacity === '0') el.style.opacity = '';
      });

      // Restructure each mega menu for mobile: nest each submenu panel inside
      // its matching category item, and drop the desktop-only featured column.
      clone.querySelectorAll('.navmenu-meganav-multi-column').forEach(function(meganav) {
        var categoryItems = meganav.querySelectorAll('.multi-col-category-item');
        var submenuPanels = meganav.querySelectorAll('.multi-col-submenu-panel');
        categoryItems.forEach(function(item, i) {
          var panel = submenuPanels[i];
          if (panel) {
            panel.classList.remove('active');
            item.appendChild(panel);
          }
          // Strip the desktop "active" highlight; mobile uses click-to-open instead
          var link = item.querySelector('.multi-col-category-link');
          if (link) link.classList.remove('active');
        });
        var featured = meganav.querySelector('.multi-col-featured');
        if (featured) featured.remove();
        var submenusWrap = meganav.querySelector('.multi-col-submenus');
        if (submenusWrap) submenusWrap.remove();
        var catTitle = meganav.querySelector('.multi-col-categories-title');
        if (catTitle) catTitle.remove();
        // Remove "View All" links inside the mobile menu
        meganav.querySelectorAll('.multi-col-submenu-link--view-all').forEach(function(el) {
          el.remove();
        });
      });

      // Click on a level-2 category toggles its nested submenu panel
      clone.querySelectorAll('.multi-col-category-link').forEach(function(link) {
        link.addEventListener('click', function(e) {
          var item = this.closest('.multi-col-category-item');
          // Only intercept if there's a nested submenu to toggle
          if (item && item.querySelector('.multi-col-submenu-panel')) {
            e.preventDefault();
            item.classList.toggle('is-open');
          }
        });
      });

      body.innerHTML = '';
      body.appendChild(clone);
      body.dataset.populated = '1';
    }

    if (drawer) drawer.classList.add('is-open');
    if (overlay) overlay.classList.add('is-open');
  };
</script>

<!-- Cart Drawer Overlay -->
<div id="custom-cart-overlay" class="custom-cart-overlay" onclick="document.getElementById('custom-cart-drawer').classList.remove('is-open'); this.classList.remove('is-open');"></div>

<!-- Cart Drawer Panel -->
<div id="custom-cart-drawer" class="custom-cart-drawer">
  <div class="custom-cart-drawer__inner">
    <!-- Header -->
    <div class="custom-cart-drawer__header">
      <span class="custom-cart-drawer__title">Your Beautiful Picks</span>
      <button class="custom-cart-drawer__close" aria-label="Close cart" onclick="document.getElementById('custom-cart-drawer').classList.remove('is-open'); document.getElementById('custom-cart-overlay').classList.remove('is-open');">
        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <line x1="18" y1="6" x2="6" y2="18"></line>
          <line x1="6" y1="6" x2="18" y2="18"></line>
        </svg>
      </button>
    </div>

    {%- unless cart.empty? -%}
      <!-- Free Shipping Banner -->
      <div class="custom-cart-drawer__shipping-banner">
        You're eligible for <strong>free UK shipping!</strong>
      </div>
    {%- endunless -%}

    <!-- Cart Content -->
    <div class="custom-cart-drawer__content">
      {%- if cart.empty? -%}
        <div class="custom-cart-drawer__empty">
          <p>Your cart is empty</p>
          <a href="{{ routes.all_products_collection_url }}" class="custom-cart-drawer__continue-btn">Continue Shopping</a>
        </div>
      {%- else -%}
        <!-- Cart Items -->
        <form action="{{ routes.cart_url }}" method="post" id="custom-cart-form">
          {%- for item in cart.items -%}
            <div class="custom-cart-drawer__item" data-line-key="{{ item.key }}" data-line-index="{{ forloop.index }}">
              <div class="custom-cart-drawer__item-image">
                {%- if item.image -%}
                  <a href="{{ item.url }}">
                    {{ item.image | image_url: width: 150 | image_tag: loading: 'lazy' }}
                  </a>
                {%- endif -%}
              </div>
              <div class="custom-cart-drawer__item-details">
                <a href="{{ item.url }}" class="custom-cart-drawer__item-title">{{ item.product.title }}</a>
                {%- if item.product.has_only_default_variant == false -%}
                  <p class="custom-cart-drawer__item-variant">
                    {%- for option in item.options_with_values -%}
                      {{ option.value }}{% unless forloop.last %} / {% endunless %}
                    {%- endfor -%}
                  </p>
                {%- endif -%}
                <div class="custom-cart-drawer__item-price">{{ item.final_price | money }}</div>
                <div class="custom-cart-drawer__quantity">
                  <button type="button" class="custom-cart-drawer__qty-btn" aria-label="Decrease quantity" data-qty-change="-1">−</button>
                  <span class="custom-cart-drawer__qty-value">{{ item.quantity }}</span>
                  <button type="button" class="custom-cart-drawer__qty-btn" aria-label="Increase quantity" data-qty-change="1">+</button>
                </div>
              </div>
              <div class="custom-cart-drawer__item-line-price">
                {{ item.final_line_price | money }}
              </div>
            </div>
          {%- endfor -%}
        </form>
      {%- endif -%}

      {%- unless cart.empty? -%}
        <!-- You May Also Like Section -->
        <div class="custom-cart-drawer__recommendations">
          <h3 class="custom-cart-drawer__rec-title">You May Also Like</h3>
          <div class="custom-cart-drawer__rec-scroll" id="cart-drawer-recommendations">

            {%- comment -%} Server-side rendered recommendations from collections {%- endcomment -%}
            {%- assign rec_product_ids = '' -%}
            {%- for item in cart.items -%}
              {%- assign rec_product_ids = rec_product_ids | append: item.product.id | append: ',' -%}
            {%- endfor -%}

            {%- assign rec_count = 0 -%}
            {%- for item in cart.items -%}
              {%- if rec_count >= 6 -%}{%- break -%}{%- endif -%}
              {%- for collection in item.product.collections -%}
                {%- if rec_count >= 6 -%}{%- break -%}{%- endif -%}
                {%- for product in collection.products limit: 10 -%}
                  {%- if rec_count >= 6 -%}{%- break -%}{%- endif -%}
                  {%- assign pid_check = product.id | append: ',' -%}
                  {%- if rec_product_ids contains pid_check -%}{%- continue -%}{%- endif -%}
                  {%- assign rec_product_ids = rec_product_ids | append: product.id | append: ',' -%}
                  {%- assign rec_count = rec_count | plus: 1 -%}
                  <div class="custom-cart-drawer__rec-card">
                    <div class="custom-cart-drawer__rec-img">
                      {%- if product.featured_image -%}
                        <a href="{{ product.url }}">
                          {{ product.featured_image | image_url: width: 200 | image_tag: loading: 'lazy' }}
                        </a>
                      {%- endif -%}
                    </div>
                    <div class="custom-cart-drawer__rec-info">
                      <span class="custom-cart-drawer__rec-price">{{ product.price | money }}</span>
                      <button class="custom-cart-drawer__rec-add" onclick="addRecToCart({{ product.variants.first.id }}, this)">Add</button>
                    </div>
                    <p class="custom-cart-drawer__rec-name">
                      <a href="{{ product.url }}">{{ product.title }}</a>
                    </p>
                  </div>
                {%- endfor -%}
              {%- endfor -%}
            {%- endfor -%}

          </div>
        </div>
      {%- endunless -%}
    </div>

    {%- unless cart.empty? -%}
      <!-- Cart Footer -->
      <div class="custom-cart-drawer__footer">
        <div class="custom-cart-drawer__total">
          <span>Total</span>
          <span class="custom-cart-drawer__total-price">{{ cart.total_price | money }}</span>
        </div>
        <p class="custom-cart-drawer__tax-note">Coupons apply at checkout.</p>
        <a href="{{ routes.cart_url }}/checkout" class="custom-cart-drawer__checkout-btn">Checkout</a>
      </div>
    {%- endunless -%}
  </div>
</div>

<script>
  // ===================== //
  // PREDICTIVE SEARCH     //
  // ===================== //
  (function() {
    var searchEl = document.querySelector('[data-ch-search]');
    if (!searchEl) return;
    var input = searchEl.querySelector('[data-ch-search-input]');
    var resultsEl = searchEl.querySelector('[data-ch-search-results]');
    var contentEl = searchEl.querySelector('[data-ch-search-content]');
    var loadingEl = searchEl.querySelector('[data-ch-search-loading]');
    var debounceTimer = null;
    var moneyFormat = {{ shop.money_format | json }};

    function formatMoney(cents) {
      var amount = (cents / 100).toFixed(2);
      var amountNoDecimals = Math.round(cents / 100);
      var amountWithComma = amount.replace('.', ',');
      return moneyFormat
        .replace(/\{\{\s*amount_with_comma_separator\s*\}\}/g, amountWithComma)
        .replace(/\{\{\s*amount_no_decimals\s*\}\}/g, amountNoDecimals)
        .replace(/\{\{\s*amount\s*\}\}/g, amount);
    }

    function escapeHtml(str) {
      if (str == null) return '';
      return String(str)
        .replace(/&/g, '&amp;')
        .replace(/</g, '&lt;')
        .replace(/>/g, '&gt;')
        .replace(/"/g, '&quot;')
        .replace(/'/g, '&#39;');
    }

    function getRecentQueries() {
      try {
        var raw = localStorage.getItem('ch_recent_queries');
        return raw ? JSON.parse(raw) : [];
      } catch(e) { return []; }
    }

    function addRecentQuery(q) {
      if (!q) return;
      var list = getRecentQueries();
      list = list.filter(function(x) { return x !== q; });
      list.unshift(q);
      list = list.slice(0, 6);
      try { localStorage.setItem('ch_recent_queries', JSON.stringify(list)); } catch(e) {}
    }

    function removeRecentQuery(q) {
      var list = getRecentQueries().filter(function(x) { return x !== q; });
      try { localStorage.setItem('ch_recent_queries', JSON.stringify(list)); } catch(e) {}
    }

    function showResults() { resultsEl.hidden = false; }
    function hideResults() { resultsEl.hidden = true; }

    function renderLeftColumn(query, collections) {
      var html = '';
      var recent = getRecentQueries();
      if (recent.length) {
        html += '<div class="ch-search-section">';
        html += '<div class="ch-search-section-title">Your Search Queries</div>';
        html += '<ul class="ch-search-list">';
        recent.forEach(function(q) {
          html += '<li class="ch-search-recent-item">' +
                  '<a href="/search?q=' + encodeURIComponent(q) + '">' + escapeHtml(q) + '</a>' +
                  '<button type="button" class="ch-search-recent-remove" data-recent-remove="' + escapeHtml(q) + '" aria-label="Remove">&times;</button>' +
                  '</li>';
        });
        html += '</ul></div>';
      }

      if (collections && collections.length) {
        html += '<div class="ch-search-section">';
        html += '<div class="ch-search-section-title">Categories</div>';
        html += '<ul class="ch-search-list">';
        collections.slice(0, 6).forEach(function(c) {
          html += '<li><a href="' + c.url + '">' + escapeHtml(c.title) + '</a></li>';
        });
        html += '</ul></div>';
      }

      return html;
    }

    function renderPopularProducts(products) {
      if (!products || !products.length) return '';
      var html = '<div class="ch-search-section">';
      html += '<div class="ch-search-section-title">Popular Products</div>';
      html += '<ul class="ch-search-list">';
      products.slice(0, 4).forEach(function(p) {
        html += '<li><a href="' + p.url + '">' + escapeHtml(p.title) + '</a></li>';
      });
      html += '</ul></div>';
      return html;
    }

    function renderRightColumn(query, products, queries) {
      var html = '<div class="ch-search-section">';
      html += '<div class="ch-search-section-title">Pages</div>';
      html += '<div class="ch-search-info">Showing <strong>' + products.length + '</strong> results for <strong>' + escapeHtml(query) + '</strong>.';
      if (queries && queries.length) html += ' Did you mean: ';
      html += '</div>';
      if (queries && queries.length) {
        html += '<div class="ch-search-suggestions-row">';
        queries.slice(0, 4).forEach(function(q) {
          var text = q.text || q;
          html += '<a href="/search?q=' + encodeURIComponent(text) + '">' + escapeHtml(text) + '</a>';
        });
        html += '</div>';
      }
      html += '</div>';

      html += '<div class="ch-search-section">';
      html += '<div class="ch-search-section-title">Products</div>';
      if (products.length) {
        html += '<div class="ch-search-products-grid">';
        products.slice(0, 4).forEach(function(p) {
          var img = p.featured_image && p.featured_image.url ? p.featured_image.url : (p.image || '');
          var price = (p.price !== undefined) ? formatMoney(p.price) : '';
          html += '<a href="' + p.url + '" class="ch-search-product-card">' +
                  '<div class="ch-search-product-card__image">' +
                  (img ? '<img src="' + img + '" alt="' + escapeHtml(p.title) + '" loading="lazy">' : '') +
                  '</div>' +
                  '<div class="ch-search-product-card__vendor">' + escapeHtml(p.vendor || '') + '</div>' +
                  '<div class="ch-search-product-card__title">' + escapeHtml(p.title) + '</div>' +
                  '<div class="ch-search-product-card__price">' + price + '</div>' +
                  '<div class="ch-search-product-card__rating">★★★★★</div>' +
                  '</a>';
        });
        html += '</div>';
      } else {
        html += '<div class="ch-search-empty">No products found.</div>';
      }
      html += '</div>';

      return html;
    }

    function render(query, data) {
      var products = (data.resources && data.resources.results && data.resources.results.products) || [];
      var collections = (data.resources && data.resources.results && data.resources.results.collections) || [];
      var queries = (data.resources && data.resources.results && data.resources.results.queries) || [];

      var html = '<div class="ch-search-wrapper">';
      html += '<div class="ch-search-left">' + renderLeftColumn(query, collections) + renderPopularProducts(products) + '</div>';
      html += '<div class="ch-search-right">' + renderRightColumn(query, products, queries) + '</div>';
      html += '</div>';
      html += '<div class="ch-search-all"><a href="/search?q=' + encodeURIComponent(query) + '">View All Results for "' + escapeHtml(query) + '"</a></div>';

      contentEl.innerHTML = html;
    }

    function renderEmpty() {
      var recent = getRecentQueries();
      if (!recent.length) {
        contentEl.innerHTML = '<div class="ch-search-empty">Start typing to search...</div>';
        return;
      }
      var html = '<div class="ch-search-wrapper">';
      html += '<div class="ch-search-left">' + renderLeftColumn('', []) + '</div>';
      html += '<div class="ch-search-right"><div class="ch-search-empty">Start typing to see results.</div></div>';
      html += '</div>';
      contentEl.innerHTML = html;
    }

    function fetchPredictive(query) {
      loadingEl.hidden = false;
      var url = '/search/suggest.json?q=' + encodeURIComponent(query) +
                '&resources[type]=product,collection,query' +
                '&resources[limit]=6&resources[options][unavailable_products]=last';
      fetch(url)
        .then(function(r) { return r.json(); })
        .then(function(data) {
          loadingEl.hidden = true;
          render(query, data);
          showResults();
        })
        .catch(function() {
          loadingEl.hidden = true;
        });
    }

    input.addEventListener('focus', function() {
      if (input.value.trim().length >= 2) {
        fetchPredictive(input.value.trim());
      } else {
        renderEmpty();
        showResults();
      }
    });

    input.addEventListener('input', function() {
      var q = input.value.trim();
      clearTimeout(debounceTimer);
      if (q.length < 2) {
        renderEmpty();
        showResults();
        return;
      }
      debounceTimer = setTimeout(function() { fetchPredictive(q); }, 250);
    });

    // Save query on submit
    searchEl.querySelector('form').addEventListener('submit', function() {
      addRecentQuery(input.value.trim());
    });

    // Remove recent query button
    contentEl.addEventListener('click', function(e) {
      var btn = e.target.closest('[data-recent-remove]');
      if (btn) {
        e.preventDefault();
        e.stopPropagation();
        removeRecentQuery(btn.getAttribute('data-recent-remove'));
        var q = input.value.trim();
        if (q.length >= 2) fetchPredictive(q); else renderEmpty();
      }
    });

    // Close on outside click
    document.addEventListener('click', function(e) {
      if (!searchEl.contains(e.target)) hideResults();
    });

    // Close on Escape
    input.addEventListener('keydown', function(e) {
      if (e.key === 'Escape') { hideResults(); input.blur(); }
    });
  })();

  // Auto-open cart drawer when item is added
  document.addEventListener('cart:add', function() {
    document.getElementById('custom-cart-drawer').classList.add('is-open');
    document.getElementById('custom-cart-overlay').classList.add('is-open');
  });

  // Format price in cents to money string
  function formatMoney(cents) {
    var moneyFormat = {{ shop.money_format | json }};
    var amount = (cents / 100).toFixed(2);
    var amountNoDecimals = Math.round(cents / 100);
    var amountWithComma = amount.replace('.', ',');
    return moneyFormat
      .replace(/\{\{\s*amount_with_comma_separator\s*\}\}/g, amountWithComma)
      .replace(/\{\{\s*amount_no_decimals\s*\}\}/g, amountNoDecimals)
      .replace(/\{\{\s*amount\s*\}\}/g, amount);
  }

  // Refresh cart drawer UI from cart JSON. Defensive against missing fields.
  function refreshCartDrawer(cartData) {
    if (!cartData || typeof cartData !== 'object') {
      console.warn('[cart] refreshCartDrawer called with invalid data', cartData);
      return;
    }

    // Update total price (only if we have a valid number)
    if (typeof cartData.total_price === 'number' && !isNaN(cartData.total_price)) {
      var totalEl = document.querySelector('.custom-cart-drawer__total-price');
      if (totalEl) totalEl.textContent = formatMoney(cartData.total_price);
    }

    // Update cart count badge
    if (typeof cartData.item_count === 'number') {
      document.querySelectorAll('[data-cart-count]').forEach(function(el) {
        el.textContent = cartData.item_count;
      });
    }

    // Update each line item — match by data-line-key first (stable across reorders), index as fallback
    var items = Array.isArray(cartData.items) ? cartData.items : [];
    items.forEach(function(item, index) {
      var itemEl = null;
      if (item && item.key) {
        itemEl = document.querySelector('[data-line-key="' + item.key + '"]');
      }
      if (!itemEl) {
        itemEl = document.querySelector('[data-line-index="' + (index + 1) + '"]');
      }
      if (!itemEl) return;

      var qtyEl = itemEl.querySelector('.custom-cart-drawer__qty-value');
      var linePriceEl = itemEl.querySelector('.custom-cart-drawer__item-line-price');
      if (qtyEl && typeof item.quantity === 'number') {
        qtyEl.textContent = item.quantity;
      }
      if (linePriceEl && typeof item.final_line_price === 'number') {
        linePriceEl.textContent = formatMoney(item.final_line_price);
      }
    });

    // Remove deleted items
    document.querySelectorAll('.custom-cart-drawer__item[data-line-key]').forEach(function(el) {
      var key = el.getAttribute('data-line-key');
      var found = items.some(function(item) { return item && item.key === key; });
      if (!found) el.remove();
    });

    // If cart empty, reload
    if (cartData.item_count === 0) window.location.reload();
  }

  // Recalculate the displayed cart total from current line prices in the DOM.
  // Used for optimistic updates so the total reflects the new line prices
  // immediately, before the server response.
  function recalcCartTotalFromDOM() {
    var totalCents = 0;
    document.querySelectorAll('.custom-cart-drawer__item-line-price').forEach(function(el) {
      totalCents += parsePriceToCents(el.textContent);
    });
    var totalEl = document.querySelector('.custom-cart-drawer__total-price');
    if (totalEl) totalEl.textContent = formatMoney(totalCents);
  }

  // Update cart quantity using line index (most reliable Shopify method)
  // Parse a money string like "Rs. 40.24" or "$12.49" or "1.234,56 €" into integer cents.
  function parsePriceToCents(text) {
    if (!text) return 0;
    var cleaned = String(text).replace(/[^0-9.,]/g, '');
    if (!cleaned) return 0;
    // Treat the LAST separator as the decimal mark (handles both "1,234.56" and "1.234,56")
    var lastSep = Math.max(cleaned.lastIndexOf('.'), cleaned.lastIndexOf(','));
    var whole, frac;
    if (lastSep >= 0 && cleaned.length - lastSep <= 3) {
      whole = cleaned.substring(0, lastSep).replace(/[.,]/g, '');
      frac = (cleaned.substring(lastSep + 1) + '00').slice(0, 2);
    } else {
      whole = cleaned.replace(/[.,]/g, '');
      frac = '00';
    }
    return (parseInt(whole, 10) || 0) * 100 + (parseInt(frac, 10) || 0);
  }

  function updateCartQty(btn, change) {
    if (!btn) return;
    var itemEl = btn.closest('.custom-cart-drawer__item');
    if (!itemEl) { console.warn('[cart] No .custom-cart-drawer__item ancestor'); return; }

    var lineIndex = parseInt(itemEl.getAttribute('data-line-index'), 10);
    if (!lineIndex) { console.warn('[cart] Missing data-line-index'); return; }

    var qtyEl = itemEl.querySelector('.custom-cart-drawer__qty-value');
    var linePriceEl = itemEl.querySelector('.custom-cart-drawer__item-line-price');
    var unitPriceEl = itemEl.querySelector('.custom-cart-drawer__item-price');
    if (!qtyEl) { console.warn('[cart] Missing .custom-cart-drawer__qty-value'); return; }

    var currentQty = parseInt(qtyEl.textContent, 10) || 1;
    var newQty = Math.max(currentQty + change, 0);
    var previousLinePriceText = linePriceEl ? linePriceEl.textContent : '';

    // Optimistic UI update — quantity, line price, AND total all change immediately
    if (newQty > 0) {
      qtyEl.textContent = newQty;
      if (linePriceEl && unitPriceEl) {
        var unitCents = parsePriceToCents(unitPriceEl.textContent);
        linePriceEl.textContent = formatMoney(unitCents * newQty);
      }
      recalcCartTotalFromDOM();
    }

    var btns = itemEl.querySelectorAll('.custom-cart-drawer__qty-btn');
    btns.forEach(function(b) { b.disabled = true; });

    fetch('/cart/change.js', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json', 'Accept': 'application/json' },
      body: JSON.stringify({ line: lineIndex, quantity: newQty })
    })
    .then(function(res) {
      // Always parse JSON — Shopify returns useful error info on 422 too
      return res.json().then(function(data) { return { ok: res.ok, status: res.status, data: data }; });
    })
    .then(function(result) {
      if (!result.ok) {
        // Shopify returned an error (e.g. 422 — stock limit, quantity rule, etc.)
        var msg = (result.data && (result.data.description || result.data.message)) || 'Cart update failed';
        console.warn('[cart] Shopify rejected the update:', msg, result.data);
        throw new Error(msg);
      }
      refreshCartDrawer(result.data);
      btns.forEach(function(b) { b.disabled = false; });
    })
    .catch(function(err) {
      console.error('[cart] Update failed —', err.message || err);
      // Revert ALL optimistic changes
      qtyEl.textContent = currentQty;
      if (linePriceEl) linePriceEl.textContent = previousLinePriceText;
      recalcCartTotalFromDOM(); // Re-sum from reverted line prices so total is correct
      btns.forEach(function(b) { b.disabled = false; });

      // Show error to user briefly (e.g. "All 1 are in your cart")
      showCartError(itemEl, err.message || 'Could not update quantity');
    });
  }

  // Show a short error toast inside the cart item
  function showCartError(itemEl, message) {
    if (!itemEl) return;
    var existing = itemEl.querySelector('.custom-cart-drawer__error');
    if (existing) existing.remove();
    var toast = document.createElement('div');
    toast.className = 'custom-cart-drawer__error';
    toast.textContent = message;
    toast.style.cssText = 'color:#c62828;font-size:12px;margin-top:6px;padding:4px 8px;background:#fdecea;border-radius:4px;';
    itemEl.appendChild(toast);
    setTimeout(function() { if (toast.parentNode) toast.remove(); }, 3000);
  }

  // Expose globally so other scripts can call it too
  window.updateCartQty = updateCartQty;

  // Event delegation — handles all qty button clicks (works for re-rendered cart items)
  document.addEventListener('click', function(e) {
    var qtyBtn = e.target.closest('.custom-cart-drawer__qty-btn');
    if (!qtyBtn || qtyBtn.disabled) return;
    var change = parseInt(qtyBtn.getAttribute('data-qty-change'), 10);
    if (!change) change = qtyBtn.getAttribute('aria-label') === 'Increase quantity' ? 1 : -1;
    e.preventDefault();
    updateCartQty(qtyBtn, change);
  });

  // Add recommended product to cart
  function addRecToCart(variantId, btn) {
    btn.disabled = true;
    btn.textContent = '...';
    fetch('/cart/add.js', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ id: variantId, quantity: 1 })
    })
    .then(function(res) { return res.json(); })
    .then(function() {
      btn.textContent = 'Added!';
      // Fetch fresh cart data to update totals
      return fetch('/cart.js').then(function(r) { return r.json(); });
    })
    .then(function(cartData) {
      refreshCartDrawer(cartData);
    })
    .catch(function() { btn.disabled = false; btn.textContent = 'Add'; });
  }
</script>

{% stylesheet %}
  .custom-header {
    background: var(--ch-bg);
    border-bottom: 1px solid var(--ch-border);
    padding: 10px 0;
    position: sticky;
    top: 0;
    z-index: 500;
  }

  /* logo | nav | icons — one row, matching the live header */
  .custom-header__inner {
    display: grid;
    grid-template-columns: auto 1fr auto;
    align-items: center;
    gap: 24px;
    min-height: 64px;
  }

  /* ---- Centre nav slot ---- */
  .custom-header__nav {
    justify-self: center;
    min-width: 0;
  }

  .custom-header__nav .site-navigation-wrapper,
  .custom-header__nav .site-navigation {
    width: auto;
  }

  /* Inside the header row the nav needs no side padding of its own */
  .custom-header__nav .site-navigation-wrapper {
    padding: 0;
  }

  /* Nav links sit on the header row, so drop the tall standalone-row padding */
  .custom-header__nav .navmenu-link {
    padding-block: 8px;
  }

  /* header-navigation renders #site-header-nav inside .site-header. Hiding that
     wrapper stops the menu flashing below the header before JS runs; once moved
     into the slot the nav is no longer its descendant, so it displays normally. */
  .site-header {
    display: none;
  }

  /* Hamburger — hidden on desktop */
  .custom-header__hamburger {
    display: none;
    background: none;
    border: none;
    padding: 4px;
    cursor: pointer;
    color: var(--ch-text);
    align-items: center;
    justify-content: center;
  }

  /* Search — a panel below the header row, opened by the magnifier icon */
  .custom-header__search {
    position: relative;
    border-top: 1px solid var(--ch-border);
    margin-top: 12px;
    padding: 14px 0 4px;
  }

  .custom-header__search[hidden] {
    display: none;
  }

  .custom-header__search-inner {
    position: relative;
  }

  .custom-header__search-toggle {
    background: none;
    border: none;
    padding: 0;
    cursor: pointer;
  }

  /* ===================== */
  /* PREDICTIVE SEARCH     */
  /* ===================== */
  .ch-search-results {
    position: absolute;
    top: calc(100% + 8px);
    left: 0;
    width: 880px;
    max-width: 92vw;
    background: #fff;
    border: 1px solid #eee;
    border-radius: 4px;
    box-shadow: 0 12px 40px rgba(0,0,0,0.12);
    z-index: 9999;
    max-height: 80vh;
    overflow-y: auto;
  }

  .ch-search-results[hidden] {
    display: none;
  }

  .ch-search-loading {
    padding: 20px;
    text-align: center;
    font-size: 13px;
    color: #888;
  }

  .ch-search-wrapper {
    display: grid;
    grid-template-columns: 240px 1fr;
  }

  .ch-search-left {
    border-right: 1px solid #f0f0f0;
    padding: 18px 18px 8px;
    background: #fafafa;
  }

  .ch-search-right {
    padding: 18px 22px 8px;
  }

  .ch-search-section {
    margin-bottom: 18px;
  }

  .ch-search-section-title {
    font-size: 11px;
    font-weight: 700;
    color: #888;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    margin-bottom: 10px;
  }

  .ch-search-list {
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .ch-search-list li {
    margin: 0 0 6px;
  }

  .ch-search-list a {
    color: #222;
    text-decoration: none;
    font-size: 13px;
    display: block;
    padding: 3px 0;
  }

  .ch-search-list a:hover {
    color: #c41e3a;
  }

  .ch-search-recent-item {
    display: flex !important;
    align-items: center;
    justify-content: space-between;
    gap: 8px;
  }

  .ch-search-recent-remove {
    background: none;
    border: none;
    color: #aaa;
    cursor: pointer;
    font-size: 14px;
    line-height: 1;
    padding: 2px 4px;
  }

  .ch-search-recent-remove:hover {
    color: #c41e3a;
  }

  .ch-search-info {
    font-size: 13px;
    color: #555;
    margin-bottom: 10px;
  }

  .ch-search-suggestions-row {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin-bottom: 6px;
  }

  .ch-search-suggestions-row a {
    background: #f3f3f3;
    color: #333;
    padding: 6px 14px;
    border-radius: 20px;
    font-size: 12px;
    text-decoration: none;
    transition: background 0.15s ease;
  }

  .ch-search-suggestions-row a:hover {
    background: #e6e6e6;
  }

  .ch-search-products-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 18px;
    margin-top: 6px;
  }

  .ch-search-product-card {
    text-decoration: none;
    color: #222;
    display: block;
  }

  .ch-search-product-card__image {
    width: 100%;
    aspect-ratio: 1;
    background: #f8f8f8;
    border-radius: 4px;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 10px;
  }

  .ch-search-product-card__image img {
    width: 100%;
    height: 100%;
    object-fit: contain;
  }

  .ch-search-product-card__vendor {
    font-size: 10px;
    text-transform: uppercase;
    color: #888;
    letter-spacing: 0.05em;
    margin-bottom: 4px;
  }

  .ch-search-product-card__title {
    font-size: 13px;
    font-weight: 600;
    color: #222;
    line-height: 1.3;
    margin-bottom: 6px;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .ch-search-product-card__price {
    font-size: 13px;
    font-weight: 700;
    color: #000;
    margin-bottom: 4px;
  }

  .ch-search-product-card__rating {
    color: #f5a623;
    font-size: 12px;
  }

  .ch-search-all {
    padding: 14px 20px 18px;
    text-align: center;
    border-top: 1px solid #f0f0f0;
  }

  .ch-search-all a {
    display: inline-block;
    background: #c41e3a;
    color: #fff;
    padding: 12px 32px;
    border-radius: 30px;
    text-decoration: none;
    font-size: 13px;
    font-weight: 600;
  }

  .ch-search-all a:hover {
    background: #a8182f;
  }

  .ch-search-empty {
    padding: 30px 20px;
    text-align: center;
    color: #888;
    font-size: 13px;
  }

  @media screen and (max-width: 1199px) {
    .ch-search-results {
      width: 720px;
    }
    .ch-search-wrapper {
      grid-template-columns: 200px 1fr;
    }
  }

  @media screen and (max-width: 999px) {
    .ch-search-results {
      width: calc(100vw - 40px);
      max-width: calc(100vw - 40px);
      left: 50%;
      transform: translateX(-50%);
    }
    .ch-search-wrapper {
      grid-template-columns: 1fr;
    }
    .ch-search-left {
      border-right: none;
      border-bottom: 1px solid #f0f0f0;
    }
    .ch-search-products-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 12px;
    }
  }

  @media screen and (max-width: 480px) {
    .ch-search-products-grid {
      grid-template-columns: 1fr;
    }
  }

  .custom-header__search-form {
    display: flex;
    align-items: center;
    border: 1px solid #ddd;
    border-radius: 30px;
    overflow: hidden;
    background: #fff;
    width: 100%;
  }

  .custom-header__search-input {
    flex: 1;
    border: none;
    outline: none;
    padding: 10px 16px;
    font-size: 13px;
    color: #666;
    background: transparent;
  }

  .custom-header__search-input::placeholder {
    color: #aaa;
  }

  .custom-header__search-btn {
    background: none;
    border: none;
    padding: 8px 14px;
    cursor: pointer;
    color: #666;
    display: flex;
    align-items: center;
  }

  .custom-header__search-btn:hover {
    color: #000;
  }

  /* Logo — left aligned */
  .custom-header__logo {
    justify-self: start;
    text-align: left;
  }

  .custom-header__logo-link {
    text-decoration: none;
    color: var(--ch-text);
  }

  .custom-header__logo-img {
    max-height: var(--ch-logo-height, 40px);
    width: auto;
    display: block;
  }

  .custom-header__logo-text {
    font-size: 24px;
    font-weight: 700;
    letter-spacing: 0.05em;
    color: var(--ch-text);
  }

  /* Icons */
  .custom-header__icons {
    justify-self: end;
    display: flex;
    align-items: center;
    gap: 22px;
  }

  .custom-header__icon {
    color: var(--ch-text);
    text-decoration: none;
    display: flex;
    align-items: center;
    transition: opacity 0.2s ease;
  }

  .custom-header__icon:hover {
    opacity: 0.6;
  }

  .custom-header__cart {
    position: relative;
  }

  .custom-header__cart-count {
    position: absolute;
    top: -7px;
    right: -9px;
    background: var(--ch-accent, #1560bd);
    color: #fff;
    font-size: 10px;
    font-weight: 600;
    width: 17px;
    height: 17px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    line-height: 1;
  }

  .custom-header__icon:hover {
    color: var(--ch-accent, #1560bd);
    opacity: 1;
  }

  .custom-header__cart-count:empty {
    display: none;
  }

  /* Responsive — tablet & mobile: hamburger | logo | icons, with search on a full-width second row */
  @media screen and (max-width: 989px) {
    .custom-header__inner {
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      gap: 12px 16px;
      padding-inline: 16px;
    }

    .custom-header__hamburger {
      display: inline-flex;
      order: 1;
    }

    .custom-header__logo {
      order: 2;
      flex: 1 1 auto;
      justify-self: center;
      text-align: center;
    }

    .custom-header__icons {
      order: 3;
      gap: 16px;
    }

    /* Hide wishlist on tablet/mobile to match the reference */
    .custom-header__icons a[aria-label="Wishlist"] {
      display: none;
    }

    /* Menu collapses into the hamburger drawer below the desktop breakpoint */
    .custom-header__nav {
      display: none;
    }

    .custom-header__search-form {
      width: 100%;
      background: #f3f3f3;
      border-color: #e6e6e6;
    }

    .custom-header__search-input {
      padding: 12px 18px;
      font-size: 14px;
    }

    .custom-header__logo-img {
      max-height: 42px;
    }
  }

  @media screen and (max-width: 480px) {
    .custom-header__logo-img {
      max-height: 36px;
    }

    .custom-header__icons {
      gap: 14px;
    }

    .custom-header__search-input {
      padding: 10px 14px;
      font-size: 13px;
    }
  }

  /* ===================== */
  /* SIDE CART DRAWER      */
  /* ===================== */

  .custom-cart-overlay {
    position: fixed;
    inset: 0;
    background: rgba(0,0,0,0.4);
    z-index: 99999;
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.3s ease, visibility 0.3s ease;
  }

  .custom-cart-overlay.is-open {
    opacity: 1;
    visibility: visible;
  }

  .custom-cart-drawer {
    position: fixed;
    top: 0;
    right: 0;
    width: 420px;
    max-width: 90vw;
    height: 100vh;
    background: #fff;
    z-index: 100000;
    transform: translateX(100%);
    transition: transform 0.3s ease;
    display: flex;
    flex-direction: column;
    box-shadow: -4px 0 20px rgba(0,0,0,0.1);
  }

  .custom-cart-drawer.is-open {
    transform: translateX(0);
  }

  .custom-cart-drawer__inner {
    display: flex;
    flex-direction: column;
    height: 100%;
    overflow: hidden;
  }

  /* Header */
  .custom-cart-drawer__header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 18px 20px;
    border-bottom: 1px solid #eee;
    flex-shrink: 0;
  }

  .custom-cart-drawer__title {
    font-size: 18px;
    font-weight: 700;
    color: #000;
  }

  .custom-cart-drawer__close {
    background: none;
    border: none;
    cursor: pointer;
    padding: 4px;
    color: #000;
    display: flex;
    align-items: center;
  }

  .custom-cart-drawer__close:hover {
    opacity: 0.6;
  }

  /* Free Shipping Banner */
  .custom-cart-drawer__shipping-banner {
    background: #000;
    color: #fff;
    text-align: center;
    padding: 10px 20px;
    font-size: 13px;
    flex-shrink: 0;
  }

  .custom-cart-drawer__shipping-banner strong {
    color: #ff4d6d;
  }

  /* Content / Items */
  .custom-cart-drawer__content {
    flex: 1;
    overflow-y: auto;
    padding: 16px 20px;
  }

  .custom-cart-drawer__empty {
    text-align: center;
    padding: 40px 0;
  }

  .custom-cart-drawer__empty p {
    font-size: 16px;
    color: #666;
    margin-bottom: 20px;
  }

  .custom-cart-drawer__continue-btn {
    display: inline-block;
    padding: 12px 28px;
    background: #000;
    color: #fff;
    text-decoration: none;
    border-radius: 4px;
    font-size: 14px;
  }

  /* Cart Item */
  .custom-cart-drawer__item {
    display: flex;
    gap: 14px;
    padding: 14px 0;
    border-bottom: 1px solid #f0f0f0;
    align-items: flex-start;
  }

  .custom-cart-drawer__item:last-child {
    border-bottom: none;
  }

  .custom-cart-drawer__item-image {
    width: 70px;
    flex-shrink: 0;
    border: 1px solid #eee;
    border-radius: 6px;
    overflow: hidden;
  }

  .custom-cart-drawer__item-image img {
    width: 100%;
    height: auto;
    display: block;
  }

  .custom-cart-drawer__item-details {
    flex: 1;
    min-width: 0;
  }

  .custom-cart-drawer__item-title {
    font-size: 13px;
    font-weight: 600;
    color: #000;
    text-decoration: none;
    display: block;
    margin-bottom: 4px;
    line-height: 1.3;
  }

  .custom-cart-drawer__item-title:hover {
    text-decoration: underline;
  }

  .custom-cart-drawer__item-variant {
    font-size: 12px;
    color: #888;
    margin: 0 0 4px;
  }

  .custom-cart-drawer__item-price {
    font-size: 13px;
    color: #333;
    margin-bottom: 8px;
  }

  .custom-cart-drawer__item-line-price {
    font-size: 14px;
    font-weight: 600;
    color: #000;
    white-space: nowrap;
    padding-top: 2px;
  }

  /* Quantity Controls */
  .custom-cart-drawer__quantity {
    display: inline-flex;
    align-items: center;
    border: 1px solid #ddd;
    border-radius: 20px;
    overflow: hidden;
  }

  .custom-cart-drawer__qty-btn {
    background: none;
    border: none;
    width: 30px;
    height: 30px;
    font-size: 16px;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #c41e3a;
    font-weight: 600;
  }

  .custom-cart-drawer__qty-btn:hover {
    background: #f5f5f5;
  }

  .custom-cart-drawer__qty-value {
    font-size: 13px;
    min-width: 28px;
    text-align: center;
    font-weight: 600;
  }

  /* You May Also Like */
  .custom-cart-drawer__recommendations {
    flex-shrink: 0;
    padding: 14px 20px;
    border-top: 1px solid #eee;
    background: #fafafa;
  }

  .custom-cart-drawer__rec-title {
    font-size: 14px;
    font-weight: 700;
    color: #000;
    margin: 0 0 12px;
  }

  .custom-cart-drawer__rec-scroll {
    display: flex;
    gap: 12px;
    overflow-x: auto;
    padding-bottom: 6px;
    scroll-snap-type: x mandatory;
    -webkit-overflow-scrolling: touch;
  }

  .custom-cart-drawer__rec-scroll::-webkit-scrollbar {
    height: 4px;
  }

  .custom-cart-drawer__rec-scroll::-webkit-scrollbar-track {
    background: #eee;
    border-radius: 2px;
  }

  .custom-cart-drawer__rec-scroll::-webkit-scrollbar-thumb {
    background: #999;
    border-radius: 2px;
  }

  .custom-cart-drawer__rec-card {
    min-width: 140px;
    max-width: 140px;
    scroll-snap-align: start;
    border: 1px solid #eee;
    border-radius: 8px;
    background: #fff;
    padding: 10px;
    flex-shrink: 0;
  }

  .custom-cart-drawer__rec-img {
    width: 100%;
    aspect-ratio: 1;
    overflow: hidden;
    border-radius: 4px;
    margin-bottom: 8px;
    background: #f5f5f5;
  }

  .custom-cart-drawer__rec-img img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .custom-cart-drawer__rec-info {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 6px;
  }

  .custom-cart-drawer__rec-price {
    font-size: 13px;
    font-weight: 700;
    color: #000;
  }

  .custom-cart-drawer__rec-add {
    background: #000;
    color: #fff;
    border: none;
    border-radius: 4px;
    padding: 4px 12px;
    font-size: 12px;
    font-weight: 600;
    cursor: pointer;
    transition: background 0.2s ease;
  }

  .custom-cart-drawer__rec-add:hover {
    background: #333;
  }

  .custom-cart-drawer__rec-add:disabled {
    opacity: 0.6;
    cursor: not-allowed;
  }

  .custom-cart-drawer__rec-name {
    font-size: 11px;
    color: #555;
    margin: 0;
    line-height: 1.3;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .custom-cart-drawer__rec-name a {
    color: inherit;
    text-decoration: none;
  }

  .custom-cart-drawer__rec-name a:hover {
    text-decoration: underline;
  }

  /* Footer */
  .custom-cart-drawer__footer {
    border-top: 1px solid #eee;
    padding: 18px 20px;
    flex-shrink: 0;
    background: #fff;
  }

  .custom-cart-drawer__total {
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 16px;
    font-weight: 700;
    margin-bottom: 6px;
  }

  .custom-cart-drawer__total-price {
    font-size: 18px;
  }

  .custom-cart-drawer__tax-note {
    font-size: 12px;
    color: #888;
    margin: 0 0 14px;
  }

  .custom-cart-drawer__checkout-btn {
    display: block;
    width: 100%;
    padding: 14px;
    background: #000;
    color: #fff;
    text-align: center;
    text-decoration: none;
    border-radius: 4px;
    font-size: 15px;
    font-weight: 600;
    border: none;
    cursor: pointer;
    transition: background 0.2s ease;
  }

  .custom-cart-drawer__checkout-btn:hover {
    background: #333;
  }

  /* Mobile adjustments */
  @media screen and (max-width: 480px) {
    .custom-cart-drawer {
      width: 100vw;
      max-width: 100vw;
    }
  }

  /* ===================== */
  /* MOBILE MENU DRAWER    */
  /* ===================== */
  .custom-mobile-menu-overlay {
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.45);
    z-index: 99999;
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.3s ease, visibility 0.3s ease;
  }

  .custom-mobile-menu-overlay.is-open {
    opacity: 1;
    visibility: visible;
  }

  .custom-mobile-menu {
    position: fixed;
    top: 0;
    left: 0;
    width: 320px;
    max-width: 85vw;
    height: 100vh;
    background: #fff;
    z-index: 100000;
    transform: translateX(-100%);
    transition: transform 0.3s ease;
    display: flex;
    flex-direction: column;
    box-shadow: 4px 0 20px rgba(0, 0, 0, 0.1);
    overflow-y: auto;
  }

  .custom-mobile-menu.is-open {
    transform: translateX(0);
  }

  .custom-mobile-menu__header {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    padding: 18px 20px 8px;
    flex-shrink: 0;
  }

  .custom-mobile-menu__title {
    display: none;
  }

  .custom-mobile-menu__close {
    background: none;
    border: none;
    cursor: pointer;
    padding: 4px;
    color: #000;
    display: flex;
    align-items: center;
  }

  .custom-mobile-menu__close:hover {
    opacity: 0.6;
  }

  .custom-mobile-menu__nav {
    padding: 0 24px 24px;
    flex: 1;
  }

  .custom-mobile-menu__list {
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .custom-mobile-menu__item {
    border-bottom: 1px solid #f2f2f2;
  }

  .custom-mobile-menu__item:last-child {
    border-bottom: none;
  }

  .custom-mobile-menu__link {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 18px 0;
    color: #000;
    text-decoration: none;
    font-size: 17px;
    font-weight: 600;
  }

  .custom-mobile-menu__item.has-children > .custom-mobile-menu__link::after {
    content: '';
    width: 10px;
    height: 10px;
    border-right: 2px solid #000;
    border-bottom: 2px solid #000;
    transform: rotate(45deg);
    margin-left: 12px;
    transition: transform 0.2s ease;
  }

  .custom-mobile-menu__item.has-children.is-open > .custom-mobile-menu__link::after {
    transform: rotate(-135deg);
  }

  .custom-mobile-menu__sublist {
    list-style: none;
    margin: 0;
    padding: 0 0 14px 12px;
    display: none;
  }

  .custom-mobile-menu__item.is-open > .custom-mobile-menu__sublist {
    display: block;
  }

  .custom-mobile-menu__sublink {
    display: block;
    padding: 10px 0;
    color: #333;
    text-decoration: none;
    font-size: 15px;
  }

  .custom-mobile-menu__sublink:hover,
  .custom-mobile-menu__link:hover {
    color: #c41e3a;
  }

  body.mobile-menu-open {
    overflow: hidden;
  }

  /* =====================================================
     Mobile drawer: override cloned desktop nav styles so
     header-navigation content stacks vertically as an
     accordion (matches the mobile menu reference design).
     ===================================================== */
  .custom-mobile-menu__nav .site-header,
  .custom-mobile-menu__nav .site-navigation-wrapper,
  .custom-mobile-menu__nav .site-navigation {
    all: unset;
    display: block;
    width: 100%;
  }

  .custom-mobile-menu__nav .navmenu,
  .custom-mobile-menu__nav .navmenu-depth-1 {
    display: block !important;
    flex-direction: column;
    align-items: stretch !important;
    width: 100%;
    margin: 0 !important;
    padding: 0 !important;
    list-style: none;
    height: auto !important;
  }

  .custom-mobile-menu__nav .navmenu-item,
  .custom-mobile-menu__nav .navmenu-item-parent,
  .custom-mobile-menu__nav .navmenu-basic__item {
    display: block !important;
    width: 100%;
    border-bottom: 1px solid #f2f2f2;
    position: static !important;
    height: auto !important;
    padding: 0 !important;
  }

  .custom-mobile-menu__nav details[data-navmenu-details] {
    display: block;
    width: 100%;
    position: static;
  }

  .custom-mobile-menu__nav .navmenu-link,
  .custom-mobile-menu__nav summary.navmenu-link {
    display: flex !important;
    width: 100%;
    justify-content: space-between;
    align-items: center;
    padding: 18px 0 !important;
    font-size: 16px !important;
    font-weight: 600;
    text-transform: none !important;
    letter-spacing: 0 !important;
    color: #000 !important;
    white-space: normal !important;
    cursor: pointer;
    list-style: none;
  }

  .custom-mobile-menu__nav summary.navmenu-link::-webkit-details-marker {
    display: none;
  }

  .custom-mobile-menu__nav details[open] > summary.navmenu-link {
    color: #c41e3a !important;
  }

  .custom-mobile-menu__nav .navmenu-icon {
    margin-left: auto;
  }

  .custom-mobile-menu__nav .navmenu-icon svg {
    width: 12px;
    height: 8px;
    transition: transform 0.2s ease;
  }

  .custom-mobile-menu__nav details[open] > summary .navmenu-icon svg {
    transform: rotate(180deg);
  }

  /* Mega menu container — flatten to inline block */
  .custom-mobile-menu__nav .navmenu-meganav,
  .custom-mobile-menu__nav .navmenu-meganav-multi-column,
  .custom-mobile-menu__nav .navmenu-meganav--desktop {
    position: static !important;
    display: block !important;
    width: 100% !important;
    max-width: 100% !important;
    transform: none !important;
    box-shadow: none !important;
    background: #f8f8f8 !important;
    padding: 0 !important;
    margin: 0 0 8px !important;
    opacity: 1 !important;
    visibility: visible !important;
    top: auto !important;
    left: auto !important;
  }

  .custom-mobile-menu__nav .multi-col-wrapper {
    display: block !important;
    grid-template-columns: none !important;
    min-height: 0 !important;
    width: 100%;
  }

  .custom-mobile-menu__nav .multi-col-categories {
    background: transparent !important;
    border: none !important;
    padding: 0 !important;
  }

  .custom-mobile-menu__nav .multi-col-category-list {
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .custom-mobile-menu__nav .multi-col-category-item {
    border-bottom: 1px solid #ececec;
    position: static;
  }

  .custom-mobile-menu__nav .multi-col-category-item:last-child {
    border-bottom: none;
  }

  .custom-mobile-menu__nav .multi-col-category-link {
    display: flex !important;
    justify-content: space-between;
    align-items: center;
    padding: 14px 18px !important;
    font-size: 15px !important;
    font-weight: 500 !important;
    color: #000 !important;
    background: transparent !important;
    border: none !important;
    text-decoration: none;
  }

  .custom-mobile-menu__nav .multi-col-category-item.is-open > .multi-col-category-link {
    color: #c41e3a !important;
  }

  .custom-mobile-menu__nav .multi-col-category-arrow {
    width: 14px !important;
    height: 14px !important;
    color: #999 !important;
    transition: transform 0.2s ease;
  }

  .custom-mobile-menu__nav .multi-col-category-item.is-open > .multi-col-category-link .multi-col-category-arrow {
    transform: rotate(90deg);
    color: #c41e3a !important;
  }

  /* Submenu panels: hidden by default, shown when their category is open */
  .custom-mobile-menu__nav .multi-col-submenu-panel {
    display: none !important;
    background: #fff;
    padding: 6px 18px 14px 30px;
  }

  .custom-mobile-menu__nav .multi-col-category-item.is-open > .multi-col-submenu-panel {
    display: block !important;
  }

  .custom-mobile-menu__nav .multi-col-submenu-grid {
    display: block !important;
    grid-template-columns: none !important;
    gap: 0 !important;
  }

  .custom-mobile-menu__nav .multi-col-submenu-link {
    display: block;
    padding: 10px 0;
    font-size: 14px;
    color: #333;
    text-decoration: none;
  }

  .custom-mobile-menu__nav .multi-col-submenu-link--view-all {
    margin-top: 8px;
    font-weight: 600;
    color: #c41e3a;
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Custom Header",
  "tag": "section",
  "class": "section-wrapper",
  "settings": [
    {
      "type": "image_picker",
      "id": "logo",
      "label": "Logo"
    },
    {
      "type": "text",
      "id": "search_placeholder",
      "label": "Search placeholder",
      "default": "Search the shiny things..."
    },
    {
      "type": "checkbox",
      "id": "show_account",
      "label": "Show account icon",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_wishlist",
      "label": "Show wishlist icon",
      "default": true
    },
    {
      "type": "url",
      "id": "wishlist_url",
      "label": "Wishlist page URL"
    },
    {
      "type": "link_list",
      "id": "mobile_menu",
      "label": "Mobile menu",
      "default": "main-menu"
    },
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background color",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text / Icon color",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "border_color",
      "label": "Border color",
      "default": "#EEEEEE"
    },
    {
      "type": "color",
      "id": "accent_color",
      "label": "Accent color",
      "info": "Used for the cart badge and icon hover.",
      "default": "#1560BD"
    },
    {
      "type": "range",
      "id": "logo_height",
      "label": "Logo height",
      "min": 20,
      "max": 80,
      "step": 2,
      "unit": "px",
      "default": 40
    }
  ],
  "presets": [
    {
      "name": "Custom Header",
      "category": "Header"
    }
  ]
}
{% endschema %}
